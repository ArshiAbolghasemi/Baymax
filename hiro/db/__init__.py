"""Database infrastructure: declarative base, engine and session handling.

Import submodules directly (``db.session``). Kept free of re-exports so
``config`` can import ``db.config`` in isolation.
"""
