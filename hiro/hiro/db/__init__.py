"""Database infrastructure: declarative base, engine and session handling.

Import submodules directly (``hiro.db.session``). Kept free of re-exports so
``hiro.config`` can import ``hiro.db.config`` in isolation.
"""
